/**
 * 小程序配置文件
 */
var appTag = 'wxf98b40bad49c1265';
var host = 'https://wujieshidai.com/RTCRoomServer/app/' + appTag;
// var host = 'http://localhost/RTCRoomServer/app/' + appTag;

var config = {
    url: `${host}`
}

module.exports = config;